# Policy Agent 项目面试问答整理

这份文档围绕当前项目，整理社招面试里最可能被问到的：

- RAG
- Agent
- Memory
- 检索
- 数据处理
- 工程化

问题尽量贴近真实面试问法，回答尽量贴近当前项目实现。

---

## 1. 这个项目是做什么的？

### 面试官可能会问
- 你这个项目整体是干什么的？
- 和普通聊天机器人有什么区别？

### 建议回答
这是一个**面向政策分析场景的 RAG Agent 系统**。  
核心能力不是闲聊，而是围绕政策文档完成：

- 政策检索
- 单篇政策摘要
- 多文档政策汇总
- 政策对比
- 多轮上下文承接

和普通聊天机器人相比，这个项目更强调：

1. **证据链**
   - 回答基于政策文本，不是纯生成
2. **任务分流**
   - 不同问题会走 retrieve / summarize / compare 等不同链路
3. **多轮能力**
   - 能记住前面提到的地区、政策对象、比较关系
4. **工程可控性**
   - 数据源、chunk、索引、检索器、回答控制都是可调试和可重建的

---

## 2. 为什么要做这个项目，而不是直接用 ChatGPT / Gemini？

### 面试官可能会问
- 通用 Agent 都这么强了，为什么还要做垂直 Agent？

### 建议回答
通用 Agent 强在泛化，但这个项目想解决的是**政策分析任务的可靠性和可控性**。

这个项目的价值在于：

1. **围绕指定政策库做回答**
   - 不是任意自由生成
2. **支持 doc 级追踪**
   - 能知道命中了哪篇政策、哪段文本
3. **支持多任务**
   - 检索、摘要、对比不是一个模式硬做
4. **支持多轮语义承接**
   - 比如先问上海，再问北京，再问“这两个地方有什么差异”
5. **工程层可复现**
   - 数据源、chunk、索引都能重建，结果更稳定

所以它更像一个可控的政策分析系统，而不是一个只会“看起来会答”的聊天助手。

---

## 3. 项目整体链路是什么？

### 面试官可能会问
- 一次请求是怎么跑的？
- Agent 的主链路怎么设计的？

### 建议回答
当前主链路大致是：

```text
用户问题
-> context resolver
-> rewrite
-> retrieve
-> strategy
-> summarize / multi_summary / compare / direct_answer
-> answer
-> judge
-> repair
-> next_step
```

其中：

- `resolver`
  - 做多轮上下文补全、指代消解、回答模式判断
- `rewrite`
  - 生成更适合检索的 query
- `retrieve`
  - 统一先检索
- `strategy`
  - 再决定后续走直接回答、单篇摘要、多文档汇总还是 compare
- `answer`
  - 基于证据生成最终回答
- `judge / repair / next_step`
  - 做质量评估、补救和后续动作建议

设计重点是：

> 统一先 retrieve，再根据证据和结构化理解结果做后置分流。

---

## 4. 为什么不是直接 retrieve 后让模型回答？

### 面试官可能会问
- 为什么要拆这么多节点？
- 直接检索+回答不就行了吗？

### 建议回答
直接 retrieve + answer 只适合简单问答，不适合政策分析这种任务。

比如：

- 单篇政策摘要
- 多篇政策汇总
- 地区间政策对比
- 场景化建议

这些都不是只看 top-k chunk 就能稳定完成的。

所以项目里拆成了不同工具层：

- `retrieve_policy`
- `summarize_policy`
- `summarize_policies`
- `compare_policy`

也就是说，retrieve 只是第一步取证据，  
后面还要根据任务类型做更深的 doc 级处理。

---

## 5. 你是怎么做切片的？

### 面试官可能会问
- 为什么这样切 chunk？
- 是按长度切的吗？
- 有 overlap 吗？

### 建议回答
这个项目不是纯按固定长度切片，而是更偏**结构化政策文档切片**。

核心思路：

1. 先做清洗
   - 去掉明显噪声、页眉页脚、空白
2. 做标题识别 / 标题路径解析
   - 保留 `title_path`
3. 再按结构段落切片

这样做的原因是：

- 政策文档天然有层级结构
- 标题本身就是很强的检索信号
- 像“支持重点”“申报条件”这种问题，靠标题路径比纯字数切片更稳

所以 chunk 不只是正文，还带：

- `title`
- `title_path`
- `title_path_str`
- `metadata`

这对后面的检索、摘要、compare 都很重要。

---

## 6. 为什么要保留标题路径？

### 面试官可能会问
- `title_path` 有什么用？

### 建议回答
标题路径在这个项目里很重要，主要有三个作用：

1. **提升检索质量**
   - 用户问“申报条件”“支持重点”时，标题路径是强信号
2. **帮助摘要抽取**
   - 单篇摘要里会按 section 抽取内容
3. **帮助 compare**
   - compare 不是纯文本比对，而是按固定 section 对比

对政策文档来说，结构信息本身就是知识。

---

## 7. 检索怎么做的？

### 面试官可能会问
- embedding 用的什么？
- 向量库是什么？
- top-k 怎么选？

### 建议回答
当前检索层主要是：

- embedding 模型
- FAISS 向量库
- payload jsonl
- retriever manifest

流程是：

```text
chunk -> embedding -> FAISS
query -> embedding -> vector search -> top-k chunks
```

`top_k` 默认一般是 5，但会根据任务场景有所差异，比如：

- 普通 retrieve：5 左右
- compare / multi-summary：可能会放宽一点

检索时并不是只拿正文，而是会拼接：

- 标题
- 标题路径
- 正文

这样能兼顾：

- 主题召回
- 结构召回
- 条款召回

---

## 8. 你怎么评估召回效果？

### 面试官可能会问
- 召回率怎么评估？
- 你怎么知道检索是好的？

### 建议回答
这个项目当前的评测重点不是直接算传统 IR 指标，而是先做**任务级结构评测**。

目前已经开始做：

- 固定样例集
- 结构字段比对
- 人工回答检查

例如会检查：

- `resolved_action`
- `retrieval_goal`
- `response_mode`
- `resolved_entities`
- `route`
- `strategy`

为什么先这样做：

因为对于 Agent/RAG 系统，很多失败并不是“没召回”，而是：

- 召回目标不对
- 比较对象选错
- 回答模式不对

后面如果继续加强评测，可以再补：

- doc 级命中率
- top-k 命中率
- compare 对象正确率

---

## 9. 多轮 memory 是怎么设计的？

### 面试官可能会问
- 你怎么做多轮上下文？
- session memory 和 working memory 区别是什么？

### 建议回答
当前 memory 是分层设计的：

1. `SessionMemory`
   - 保存原始对话历史
2. `WorkingMemory`
   - 保存当前任务状态
3. `MemoryEntity`
   - 保存最近提到的对象
4. `ComparisonMemory`
   - 保存当前比较对象组

区别是：

- `SessionMemory` 更像原始历史记录
- `WorkingMemory` 更像供下一轮推理消费的结构化上下文

例如 `WorkingMemory` 里会记：

- 当前地区
- 当前主题
- 当前摘要/对比状态
- 最近候选政策
- 最近实体对象
- 当前比较对象组

---

## 10. 为什么不直接全交给 LLM 做 memory？

### 面试官可能会问
- 为什么还要存结构化 memory？

### 建议回答
因为全交给 LLM 虽然灵活，但会有几个问题：

1. 成本高
2. 上下文窗口压力大
3. 调试困难
4. 一致性差

所以当前做法是：

- **结构化 memory 存状态**
- **LLM resolver 负责读这些状态并做语义理解**

也就是说：

> memory 负责“记住”  
> resolver 负责“理解”

这是效果和工程可控性的平衡。

---

## 11. resolver 是做什么的？

### 面试官可能会问
- resolver 和 planner 有什么区别？
- 为什么要做 resolver？

### 建议回答
resolver 是现在主链里的**主理解入口**。

它负责：

- 多轮上下文补全
- 指代消解
- 比较对象扩展
- 任务类型理解
- 回答模式判断

当前它会输出结构化结果，例如：

- `resolved_action`
- `response_mode`
- `retrieval_goal`
- `focus`
- `answer_plan`
- `resolved_entities`
- `contextualized_query`

也就是说，它不只是“补 query”，而是在做：

**query understanding + response planning**

---

## 12. 现在 `planner` 还有什么作用？

### 面试官可能会问
- 你不是有 resolver 了吗，planner 还有什么用？

### 建议回答
一开始 `planner` 是主理解节点，  
现在随着 resolver 变强，`planner` 正在退成：

- 执行默认值补全器
- fallback

也就是说：

- resolver 决定“这轮是什么任务、怎么答、在比谁”
- planner 只补：
  - `needs_rag`
  - `needs_rewrite`
  - `answer_style`
  - 初始 route

这是架构在演进中的一部分。

---

## 13. compare 为什么容易出问题？你是怎么修的？

### 面试官可能会问
- 多轮 compare 怎么处理？
- 为什么会串到别的地区？

### 建议回答
compare 容易出问题的根因是：

1. 用户问题里有省略/指代
   - 比如“这两个地方”“安徽和它们比”
2. 如果系统没有记住比较对象组
   - 就会退回到当前检索结果里临时猜对象
3. 一猜，就容易串到别的文档或地区

我当前的修法是：

- 在 memory 里增加：
  - `recent_entities`
  - `active_comparison`
- 让 resolver 负责：
  - 把“这两个地方”解析成上一轮的地区组
  - 把“安徽和它们比”扩展成三对象比较集合

这比简单靠 query 关键词更稳。

---

## 14. 你的 compare 回答为什么还要做回答模式控制？

### 面试官可能会问
- compare 不就是比较一下吗，为什么还要 `response_mode`？

### 建议回答
因为“compare”只是任务类型，还不够表达用户真正想要的回答方式。

例如：

- `比较北京和上海的大模型政策`
  - 更像普通政策横向对比
- `具体在哪些场景下适合上海？哪些场景北京更好？`
  - 更像场景化建议

所以需要：

- `response_mode`
  - 区分普通 compare
  - 和场景建议 compare

否则 answer 容易统一退回八股文政策综述。

---

## 15. 你怎么解决“一句话里有多个问题”的情况？

### 面试官可能会问
- 如果用户一轮里既问差异又问哪个好，怎么处理？

### 建议回答
这个问题一开始是项目里的一个痛点。

早期回答容易：

- 只抓住一个点
- 比如只答“哪个好”
- 忽略“差异”

所以后来我加了一层：

- `answer_plan`

它会告诉 answer：

- 这轮必须覆盖哪些点
- 是否需要举例
- 是否需要明确推荐
- 是否应当先讲差异，再给建议

这样 answer 不再只靠单一模板，而是按回答计划组织输出。

---

## 16. 为什么还保留规则 fallback？

### 面试官可能会问
- 你不是强调 LLM 主路径了吗，为什么还有规则？

### 建议回答
主链的理解和回答现在优先走 LLM，
规则更多保留在：

- fallback
- 简单快捷路径
- 无 LLM 环境兜底

例如：

- 没有 API key
- LLM 调用失败
- 极简单的“第二篇呢”这种短路径

这样能兼顾：

- 效果
- 可用性
- 成本和调试

---

## 17. 你怎么处理 PDF 数据质量问题？

### 面试官可能会问
- PDF 出乱码怎么办？
- 原始数据源不稳定怎么办？

### 建议回答
这个项目里真实遇到过 PDF 抽取问题，尤其是北京文档。

处理方式有几层：

1. `pdf_loader` 支持多种抽取尝试
2. 对明显乱码文本做检测
3. 如果能拿到官方正文，优先迁移到 `txt`
4. `loader_factory` 支持同一 `doc_id` 多源共存，由 metadata 决定主源
5. 提供：
   - `source_audit.py`
   - `rebuild_data.py`
   来保证数据迁移和索引重建可控

这个问题本身也是项目很有工程价值的一部分。

---

## 18. 改了数据源以后，系统怎么更新？

### 面试官可能会问
- 你改了 txt / metadata 后，怎么让检索生效？

### 建议回答
项目里专门做了：

- [rebuild_data.py](C:\D\Agent-learn\MyProject\scripts\rebuild_data.py)

它会重建：

- `policy_chunks.jsonl`
- `policy_payloads.jsonl`
- `policy_chunks.faiss`
- `policy_retriever_manifest.json`

也就是说：

- raw 数据源
- metadata
- chunk
- 检索索引

这条链是可重建的，不是靠手工刷新缓存。

---

## 19. 这个项目现在还存在哪些不足？

### 面试官可能会问
- 如果继续做，你会怎么迭代？

### 建议回答
我认为当前主要还有这几块可以继续加强：

1. compare 工具还偏双对象
2. 多对象 compare 目前更多是 resolver 级支持，还没完全下沉到工具层
3. `router.py` / `strategy.py` 仍保留部分 fallback 规则
4. 评测集和指标体系还可以更完整
5. 调用链较长，后面可以优化延迟和模型调用次数

也就是说，当前已经是一个比较完整的原型系统，但还没做到完全产品级。

---

## 20. 这个项目最大的亮点是什么？

### 建议回答
我觉得最大亮点有三个：

1. **不是 naive RAG**
   - 有 resolver、strategy、judge、repair、next_step 等多阶段控制

2. **多轮语义理解不是只靠历史拼接**
   - 做了 memory 分层、entity memory、comparison memory

3. **从数据、检索、Agent 到回答控制是打通的**
   - 不只是调模型 API，而是完整理解整个系统怎么工作

---

## 最后一段可以怎么总结项目

如果面试官让你一句话总结这个项目，可以说：

> 我做的是一个面向政策分析场景的 RAG Agent 系统，不只是做检索问答，还支持单篇摘要、多文档汇总、政策对比和多轮上下文理解；项目重点放在结构化 resolver、memory、证据驱动回答和工程化可重建链路上。

